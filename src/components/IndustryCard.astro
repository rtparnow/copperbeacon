---
interface Props {
  industry: string;
  description: string;
  website?: string;
  websiteLabel?: string;
}

const { industry, description, website, websiteLabel } = Astro.props;
---

<div class="card flex flex-col">
  <h3 class="font-serif font-medium text-navy mb-2" style="font-size: 1.25rem; line-height: 1.3;">
    {industry}
  </h3>
  <p class="font-sans text-charcoal/80 text-sm leading-relaxed flex-grow mb-4">
    {description}
  </p>
  {website && websiteLabel && (
    <a
      href={`https://${website}`}
      class="font-sans text-sm font-medium text-copper hover:text-copper-dark transition-colors duration-200 inline-flex items-center gap-1"
      target="_blank"
      rel="noopener noreferrer"
    >
      {websiteLabel}
      <svg width="14" height="14" viewBox="0 0 14 14" fill="none" aria-hidden="true">
        <path d="M2.5 7h9M7.5 3l4 4-4 4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
      </svg>
    </a>
  )}
</div>
